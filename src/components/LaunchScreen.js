import React,{useState, useEffect} from 'react';

const LaunchScreen = () =>{
return(<div className='launchScreen'><h1>Air Quality Index based on your location</h1></div>);
};
export default LaunchScreen;